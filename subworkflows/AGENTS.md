# Subworkflows

`subworkflows/nf-core/**` is installed component state; use `modules.json` and the pipeline contribution
procedure for installs, updates, or patches. Do not hand-edit installed subworkflows as local composition.

`subworkflows/local/**` is repository-authored composition. Read `subworkflows/local/AGENTS.md` before changing
it.
