# Pipeline workflows

This subtree owns pipeline orchestration: route selection, pipeline-wide channel composition, reuse identity,
publication routing, and run-level aggregation. Apply the pipeline Nextflow standards indexed by
`CODING_STANDARDS.md` and use `nf-core-pipeline-contribute` for contribution procedure.

Do not move orchestration or scientific/publication policy into an installed or upstream-bound atomic
component.
