/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/gwas Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    LDAK-KVIK binary-trait route test options
------------------------------------------------------------------------------------------
*/

route_profile_template = 'samplesheet_ldak_kvik_binary.csv'
includeConfig 'test_route_profile_resolver.config'

process {
    resourceLimits = [cpus: 4, memory: '8.GB', time: '1.h']

    // LDAK-KVIK estimates stochastic model components. Pin its documented seed for snapshots.
    withName: 'LDAK_KVIKSTEP1|LDAK_KVIKSTEP2' {
        ext.args = [
            '--mpheno 1',
            '--random-seed 7',
        ].join(' ').trim()
    }
}

params {
    // Config options
    config_profile_name        = 'LDAK-KVIK binary-trait test profile'
    config_profile_description = 'Binary LDAK-KVIK route using every predictor'

}
