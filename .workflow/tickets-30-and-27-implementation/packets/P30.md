# P30: Pipeline residue and broken test setup

Implement ticket #30 in the current pipeline checkout. Remove only the retired
`PLINK_GWAS` and `PLINK_VCF` component artifacts, prove supported PLINK1 input
and PLINK2 VCF routes remain referenced, repair the two named GCTA nf-test
setup paths, compare pipeline lint to baseline, run focused tests, and commit.
Preserve unrelated user-owned files.
