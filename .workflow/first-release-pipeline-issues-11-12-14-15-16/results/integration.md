# Integration result

Status: complete at the final integration head; focused, broad, sequential,
Standards, and Spec gates are green. The exact reviewed head is landed on
`claude`.

## Integrated state

- Pinned pre-wave fixed point:
  `21b5576da6f77d23e579bf0ed7140250280a5439`.
- Final integration head:
  `7554a56728f1fa12b0e8935e9ce5183db590f194`.
- Integration branch:
  `codex/integration-wave-11-16-RptYgU`.
- Target branch:
  `claude` at
  `7554a56728f1fa12b0e8935e9ce5183db590f194`.
- Target/integration divergence after fast-forward landing: `0/0`.
- The integrated range contains 36 commits.
- The integration worktree is clean and `git diff --check` passes for the
  complete fixed-point-to-head range.

## Accepted

- One shared 35-column input foundation for all three streams, with
  method-conditioned defaults and validation for LDAK weights and the three
  GCTA construction settings.
- Association routes for REGENIE and LDAK-KVIK, including standard/chunked and
  quantitative/binary selectors, native results, canonical harmonisation,
  method-specific publication, stable identity, deterministic test seeds, and
  portable registered profiles.
- GCTA GREML-LDMS and fastGWA-MLM routes, preserving matrix-family identity,
  deterministic stratum/MGRM order, separate focal-analysis identity, native
  output, and canonical summary statistics.
- LDAK relatedness preparation and REML, including equal or content-keyed
  supplied weights, filtered/unfiltered fan-out, liability output, and reuse
  coverage.
- Registered immutable route-profile samplesheets in place of
  `when.params.input` mutation, uniform real/stub scenario pairing, labelled
  path/content snapshots, and normalized configuration banners/assertion
  grouping.

## Remediation accepted after initial integration

- `b327c2b` aligned fixture validation and route documentation with standards.
- `4187107` repaired complete LDMS MAF coverage by requiring boundaries from
  0 through 0.5; the number of intervals remains one less than the number of
  boundaries.
- `d38143a`, `79268df`, `e0bbb06`, and `1785d9d` moved route scenarios through
  registered profiles, preserved exact KVIK seeds, paired LDAK snapshots, and
  normalized final mechanical standards details.
- `a9d9ed2` made the exact `test_regenie` and `test_ldak` umbrella profiles
  portable aliases of their registered scenario profiles.
- `67fc975` kept broad fixture routes executable under the expanded method
  topology, including test-only unconstrained handling for tiny GCTA
  GREML-LDMS fixtures while production and dedicated route behavior remains
  constrained.
- `2cd6be9` updated default-route expectations and snapshots for all newly
  active methods.
- `bfab3a2` fixed the canonical KVIK contract by joining LDAK's companion
  summary data on Predictor and alleles to supply unambiguous `EAF` and
  effective `N`, while leaving native output unchanged.
- `cee2071` and `48d6e65` deliberately refreshed downstream route snapshots
  and expectations after the canonical KVIK fix.
- `7554a56` applied the final reviewed baseline KVIK summary snapshot delta
  exposed by the exact broad run.

## Rejected or superseded

- A 31-column compatibility shim was not accepted. The user authorized the
  exact 35-column public contract, and legacy 31-column input is rejected with
  the four missing fields named.
- Basename-only or absolute-path LDAK weights identity was rejected in favor of
  a content-derived digest.
- Treating different LD-bin counts as a same-key conflict was rejected:
  `gcta_ld_bins` is scientific matrix identity, so different counts correctly
  create different builds.
- The provisional quantitative-only fastGWA review boundary is superseded by a
  genuine Docker MLM-binary route proving live GCTA's native `BETA` and `N`
  columns and their canonical mapping.
- Provisional snapshot differences were not accepted wholesale; snapshots were
  refreshed only after matching route-output, generated-command, or canonical
  header evidence.

## Conflicts resolved

- Shared workflow, dispatch, configuration, schema, documentation, and snapshot
  edits were replayed as coherent integration commits rather than overwriting
  one stream with another.
- The LDMS test fixture uses a single LD interval because of its tiny marker
  count, while production defaults retain four bins.
- GCTA's constrained production behavior is preserved; only the broad
  nf-test fixture topology receives the test-only unconstrained override needed
  for tiny data.
- Native LDAK-KVIK association output remains untouched. A
  harmonisation-only adapter supplies the explicit A1 frequency and effective
  analysis size required by the canonical schema.

## Remaining integration risk

No unresolved integration or review finding is known. The exact fixture-backed
three-shard suite and the decisive supported-offline sequential audit are both
green at 116/116, and the audit reports no obsolete snapshot warnings or
labels. No implementation or validation risk remains open. The only remaining
archival step is an explicit records-only commit for the finalized workflow and
issue files; it is not an implementation or validation gate.
