# Distill nf-core subworkflow conventions

Goal: derive evidence-backed conventions from `.references/modules/subworkflows/nf-core/` and add durable guidance to this repository's nf-core skills.

Success criteria:

- Cover naming/layout, public contracts/metadata, composition/versioning, and tests/configuration.
- Support guidelines with repeated companion-library evidence or explicit local companion documentation.
- Separate broad conventions from exceptions and tool-specific patterns.
- Put each accepted rule in one appropriate skill source of truth.
- Preserve unrelated changes in both the pipeline and companion repositories.
- Validate edited skill Markdown and review the integrated guidance against the evidence.

Constraints: agents research read-only; the primary agent owns all skill edits and integration. The dirty companion checkout is evidence only and must not be modified.
