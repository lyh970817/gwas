# Naming and layout

Research format-first naming, directory/workflow/meta token consistency, includes, structural order, core files, and exceptions across unrelated established companion subworkflows. Read-only; return cited candidate rules and rejected weak observations.
