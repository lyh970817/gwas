# Packet 01: Tower report metadata

Compare `tower.yml` in Sarek, MAG, RNA-seq, and this pipeline. Determine which
report paths may truthfully be advertised. Record evidence, recommendation,
anti-patterns, and validation in `results/tower.md`. Read-only research only.
