.nextflow*
work/
data/
results/
.DS_Store
testing/
testing*
*.pyc
null/
.lineage/

# Local development environment — machine-specific, and .envrc carries secrets
.envrc
.direnv/
.nf-test*

# Local agent and workspace files — mutable client state is not part of the
# nf-core pipeline. The shared skills and repository instructions are tracked
# through .agents/skills, .claude/skills, .codex/skills, and AGENTS.md.
.claude/*
!.claude/skills
SPEC.md
.scratch/
.lavish/
.worktrees/
# Local reference checkouts used for style comparison, component development,
# and fixture-backed testing. They are intentionally machine-local.
.references/
CONTEXT.md
CONTEXT-MAP.md
docs/agents/
docs/adr/
docs/gwas-tool-documentation/
docs/nf-core-standards/
