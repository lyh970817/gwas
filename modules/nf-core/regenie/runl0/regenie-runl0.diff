Changes in component 'nf-core/regenie/runl0'
Changes in 'regenie/runl0/main.nf':
--- modules/nf-core/regenie/runl0/main.nf
+++ modules/nf-core/regenie/runl0/main.nf
@@ -45,11 +45,19 @@
     """
 
     stub:
+    def args = task.ext.args ?: ''
     def input_prefix = plink_genotype_file.baseName
     def prefix = task.ext.prefix ?: input_prefix
     def run_prefix = "${prefix}_job${job_number}"
+    // A multi-phenotype L0 job writes one level-0 prediction block per `--phenoColList` column, numbered in
+    // REGENIE's own order, so a stub emitting only `_l0_Y1` reports one-phenotype cardinality for a
+    // multi-phenotype fit. The column list is optionally single-quoted on the command line. Without the
+    // option the stub keeps its single block.
+    def pheno_match = args =~ /--phenoColList\s+'?([^'\s]+)'?/
+    def pheno_columns = pheno_match.find() ? pheno_match.group(1).tokenize(',') : ['Y1']
+    def l0_lines = (1..pheno_columns.size()).collect { index -> "touch ${run_prefix}_l0_Y${index}" }.join('\n    ')
     """
-    touch ${run_prefix}_l0_Y1
+    ${l0_lines}
     touch ${run_prefix}.log
     """
 }

'modules/nf-core/regenie/runl0/environment.yml' is unchanged
'modules/nf-core/regenie/runl0/meta.yml' is unchanged
Changes in 'regenie/runl0/tests/main.nf.test':
--- modules/nf-core/regenie/runl0/tests/main.nf.test
+++ modules/nf-core/regenie/runl0/tests/main.nf.test
@@ -202,6 +202,59 @@
 
     }
 
+    test("homo_sapiens popgen - multiple phenotype columns - stub") {
+
+        options "-stub"
+
+        when {
+            params {
+                module_args = "--phenoColList 'TraitA,TraitB'"
+            }
+            process {
+                """
+                input[0] = [
+                    [ id:'plink_simulated' ],
+                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/popgen/plink_simulated.bed', checkIfExists: true),
+                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/popgen/plink_simulated.bim', checkIfExists: true),
+                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/popgen/plink_simulated.fam', checkIfExists: true)
+                ]
+
+                input[1] = REGENIE_SPLITL0.out.master
+                    .combine(REGENIE_SPLITL0.out.snplists)
+                    .map { master_meta, master, snplist_meta, snplists ->
+                        [ master_meta, master, snplists, 1 ]
+                    }
+
+                input[2] = [
+                    [ id:'plink_simulated' ],
+                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/popgen/plink_simulated_quantitative_phenoname.phe', checkIfExists: true)
+                ]
+
+                input[3] = [
+                    [ id:'plink_simulated' ],
+                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/popgen/plink_simulated_covariates.txt', checkIfExists: true)
+                ]
+
+                input[4] = 100
+                """
+            }
+        }
+
+        then {
+            // A real L0 job writes one level-0 block per phenotype column, numbered in REGENIE's own
+            // order. The assertion is keyed on that numbering rather than on a full basename, because
+            // `ext.prefix` is the caller's.
+            def predictionNames = process.out.l0_predictions.get(0).get(1).collect { prediction -> path(prediction).getFileName().toString() }
+            assertAll(
+                { assert process.success },
+                { assert predictionNames.size() == 2 },
+                { assert predictionNames.count { name -> name.endsWith('_job1_l0_Y1') } == 1 },
+                { assert predictionNames.count { name -> name.endsWith('_job1_l0_Y2') } == 1 },
+            )
+        }
+
+    }
+
     test("homo_sapiens popgen - quantitative plink2 without covariates and default bsize") {
 
         when {

'modules/nf-core/regenie/runl0/tests/main.nf.test.snap' is unchanged
'modules/nf-core/regenie/runl0/tests/nextflow.config' is unchanged
************************************************************
