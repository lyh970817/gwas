Changes in component 'nf-core/gcta/bivariateremlldms'
Changes in 'gcta/bivariateremlldms/main.nf':
--- modules/nf-core/gcta/bivariateremlldms/main.nf
+++ modules/nf-core/gcta/bivariateremlldms/main.nf
@@ -7,7 +7,7 @@
         : 'community.wave.seqera.io/library/gcta:1.94.1--9bc35dc424fcf6e9'}"

     input:
-    tuple val(meta), path(mgrm_file), path(grm_files)
+    tuple val(meta), path(grm_files), val(grm_prefixes)
     tuple val(meta2), path(phenotype_file), val(phenotype_col1), val(phenotype_col2)
     tuple val(meta3), path(quant_covariates_file)
     tuple val(meta4), path(cat_covariates_file)
@@ -23,13 +23,17 @@
     script:
     def args = task.ext.args ?: ''
     def prefix = task.ext.prefix ?: "${meta.id}"
+    def mgrm_entries = grm_prefixes.collect { grm_prefix -> "\"${grm_prefix}\"" }.join(' ')
+    def mgrm_file = "${prefix}.mgrm"
     def reml_bivar_param = phenotype_col1 && phenotype_col2 ? "--reml-bivar ${phenotype_col1} ${phenotype_col2}" : "--reml-bivar"
     def qcovar_param = quant_covariates_file ? "--qcovar ${quant_covariates_file}" : ''
     def covar_param = cat_covariates_file ? "--covar ${cat_covariates_file}" : ''
     """
+    printf '%s\n' ${mgrm_entries} > "${mgrm_file}"
+
     gcta \\
         ${reml_bivar_param} \\
-        --mgrm ${mgrm_file} \\
+        --mgrm "${mgrm_file}" \\
         --pheno "${phenotype_file}" \\
         ${qcovar_param} \\
         ${covar_param} \\

'modules/nf-core/gcta/bivariateremlldms/environment.yml' is unchanged
Changes in 'gcta/bivariateremlldms/meta.yml':
--- modules/nf-core/gcta/bivariateremlldms/meta.yml
+++ modules/nf-core/gcta/bivariateremlldms/meta.yml
@@ -1,6 +1,6 @@
 # yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/meta-schema.json
 name: "gcta_bivariateremlldms"
-description: Run bivariate REML-LDMS analysis with an MGRM manifest
+description: Run bivariate REML-LDMS analysis from an explicitly ordered GRM family
 keywords:
   - gcta
   - genome-wide complex trait analysis
@@ -24,21 +24,16 @@
   - - meta:
         type: map
         description: |
-          Groovy map containing MGRM metadata
-          `meta.id` should match the MGRM manifest basename, for example
-          `plink_simulated_ldms` for `plink_simulated_ldms.mgrm`
-    - mgrm_file:
-        type: file
-        description: MGRM manifest file
-        pattern: "*.mgrm"
-        ontologies:
-          - edam: "http://edamontology.org/format_2330"
+          Groovy map containing GRM family metadata
+          `meta.id` supplies the overridable default output prefix
     - grm_files:
         type: file
-        description: GRM bundles referenced by `mgrm_file`; each manifest entry
-          should have matching `*.grm.{id,bin,N.bin}` files in this bundle
+        description: GRM bundles staged beside the task-local MGRM manifest
         pattern: "*.grm.*"
         ontologies: []
+    - grm_prefixes:
+        type: list
+        description: GRM basenames in the explicit component order written to the task-local MGRM manifest
   - - meta2:
         type: map
         description: |

Changes in 'gcta/bivariateremlldms/tests/main.nf.test':
--- modules/nf-core/gcta/bivariateremlldms/tests/main.nf.test
+++ modules/nf-core/gcta/bivariateremlldms/tests/main.nf.test
@@ -89,7 +89,7 @@
         }

         run("GCTA_MAKEGRMPART", alias: "GCTA_MAKEGRMPART_LDMS1") {
-            script "../../makegrmpart/main.nf"
+            script "../../../../local/gcta/makegrmpart/main.nf"
             process {
                 """
                 file('plink_simulated.mbfile').text = 'plink_simulated\\n'
@@ -118,7 +118,7 @@
         }

         run("GCTA_MAKEGRMPART", alias: "GCTA_MAKEGRMPART_LDMS2") {
-            script "../../makegrmpart/main.nf"
+            script "../../../../local/gcta/makegrmpart/main.nf"
             process {
                 """
                 file('plink_simulated.mbfile').text = 'plink_simulated\\n'
@@ -148,19 +148,15 @@
         when {
             process {
                 """
-                mgrm_file = Channel
-                    .of('plink_simulated_ldms1.part_1_1\\nplink_simulated_ldms2.part_1_1')
-                    .collectFile(name:'plink_simulated_ldms.mgrm', newLine: true)
-
                 ldms_grm_files = GCTA_MAKEGRMPART_LDMS1.out.grm_files
                     .mix(GCTA_MAKEGRMPART_LDMS2.out.grm_files)
                     .map { meta, grm_files, nparts_gcta, part_gcta_job -> grm_files }
                     .collect()
                     .map { rows -> rows.flatten() }

-                input[0] = mgrm_file
-                    .combine(ldms_grm_files)
-                    .map { row -> [[ id:'plink_simulated_ldms' ], row[0], row[1..-1]] }
+                input[0] = ldms_grm_files.map { grm_files ->
+                    [[ id:'plink_simulated_ldms' ], grm_files, ['plink_simulated_ldms1.part_1_1', 'plink_simulated_ldms2.part_1_1']]
+                }
                 input[1] = GAWK_BIVARIATE_PHENO.out.output.map { meta, phenotype_file -> [[ id:'plink_simulated_ldms' ], phenotype_file, 1, 2] }
                 input[2] = GAWK_QUANTITATIVE_COVARIATES.out.output.map { meta, quant_covariates_file -> [[ id:'plink_simulated_ldms' ], quant_covariates_file] }
                 input[3] = GAWK_CATEGORICAL_COVARIATES.out.output.map { meta, cat_covariates_file -> [[ id:'plink_simulated_ldms' ], cat_covariates_file] }
@@ -169,6 +165,8 @@
         }

         then {
+            def workDirectory = path(process.out.log_file.get(0).get(1)).parent
+            def mgrm = workDirectory.resolve('plink_simulated_ldms.mgrm').text
             assertAll(
                 { assert process.success },
                 { assert process.out.bivariate_results.size() == 1 },
@@ -177,6 +175,7 @@
                 { assert file(process.out.bivariate_results.get(0).get(1)).name == "plink_simulated_ldms.hsq" },
                 { assert file(process.out.log_file.get(0).get(1)).name == "plink_simulated_ldms.log" },
                 { assert file(process.out.log_file.get(0).get(1)).exists() },
+                { assert mgrm == 'plink_simulated_ldms1.part_1_1\nplink_simulated_ldms2.part_1_1\n' },
                 {
                     def resultFile = file(process.out.bivariate_results.get(0).get(1))
                     def parsedRows = resultFile.readLines()
@@ -209,19 +208,15 @@
             }
             process {
                 """
-                mgrm_file = Channel
-                    .of('plink_simulated_ldms1.part_1_1\\nplink_simulated_ldms2.part_1_1')
-                    .collectFile(name:'plink_simulated_ldms.mgrm', newLine: true)
-
                 ldms_grm_files = GCTA_MAKEGRMPART_LDMS1.out.grm_files
                     .mix(GCTA_MAKEGRMPART_LDMS2.out.grm_files)
                     .map { meta, grm_files, nparts_gcta, part_gcta_job -> grm_files }
                     .collect()
                     .map { rows -> rows.flatten() }

-                input[0] = mgrm_file
-                    .combine(ldms_grm_files)
-                    .map { row -> [[ id:'plink_simulated_ldms' ], row[0], row[1..-1]] }
+                input[0] = ldms_grm_files.map { grm_files ->
+                    [[ id:'plink_simulated_ldms' ], grm_files, ['plink_simulated_ldms1.part_1_1', 'plink_simulated_ldms2.part_1_1']]
+                }
                 input[1] = GAWK_BIVARIATE_BINARY_PHENO.out.output.map { meta, phenotype_file -> [[ id:'plink_simulated_ldms' ], phenotype_file, '', ''] }
                 input[2] = [[ id:'plink_simulated_ldms' ], []]
                 input[3] = [[ id:'plink_simulated_ldms' ], []]
@@ -250,19 +245,15 @@
         when {
             process {
                 """
-                broken_mgrm_file = Channel
-                    .of('plink_simulated_ldms_missing.part_1_1')
-                    .collectFile(name:'plink_simulated_ldms_broken.mgrm', newLine: true)
-
                 ldms_grm_files = GCTA_MAKEGRMPART_LDMS1.out.grm_files
                     .mix(GCTA_MAKEGRMPART_LDMS2.out.grm_files)
                     .map { meta, grm_files, nparts_gcta, part_gcta_job -> grm_files }
                     .collect()
                     .map { rows -> rows.flatten() }

-                input[0] = broken_mgrm_file
-                    .combine(ldms_grm_files)
-                    .map { row -> [[ id:'plink_simulated_ldms' ], row[0], row[1..-1]] }
+                input[0] = ldms_grm_files.map { grm_files ->
+                    [[ id:'plink_simulated_ldms' ], grm_files, ['plink_simulated_ldms_missing.part_1_1']]
+                }
                 input[1] = GAWK_BIVARIATE_PHENO.out.output.map { meta, phenotype_file -> [[ id:'plink_simulated_ldms' ], phenotype_file, '', ''] }
                 input[2] = [[ id:'plink_simulated_ldms' ], []]
                 input[3] = [[ id:'plink_simulated_ldms' ], []]
@@ -284,19 +275,15 @@
         when {
             process {
                 """
-                mgrm_file = Channel
-                    .of('plink_simulated_ldms1.part_1_1\\nplink_simulated_ldms2.part_1_1')
-                    .collectFile(name:'plink_simulated_ldms.mgrm', newLine: true)
-
                 ldms_grm_files = GCTA_MAKEGRMPART_LDMS1.out.grm_files
                     .mix(GCTA_MAKEGRMPART_LDMS2.out.grm_files)
                     .map { meta, grm_files, nparts_gcta, part_gcta_job -> grm_files }
                     .collect()
                     .map { rows -> rows.flatten() }

-                input[0] = mgrm_file
-                    .combine(ldms_grm_files)
-                    .map { row -> [[ id:'plink_simulated_ldms' ], row[0], row[1..-1]] }
+                input[0] = ldms_grm_files.map { grm_files ->
+                    [[ id:'plink_simulated_ldms' ], grm_files, ['plink_simulated_ldms1.part_1_1', 'plink_simulated_ldms2.part_1_1']]
+                }
                 input[1] = GAWK_BIVARIATE_PHENO.out.output.map { meta, phenotype_file -> [[ id:'plink_simulated_ldms' ], phenotype_file, '', ''] }
                 input[2] = [[ id:'plink_simulated_ldms' ], []]
                 input[3] = [[ id:'plink_simulated_ldms' ], []]

'modules/nf-core/gcta/bivariateremlldms/tests/main.nf.test.snap' is unchanged
'modules/nf-core/gcta/bivariateremlldms/tests/nextflow.config' is unchanged
************************************************************
