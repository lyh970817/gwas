# Modules

`modules/nf-core/**` is installed component state; use `modules.json` and the pipeline contribution procedure
for installs, updates, or patches. Do not hand-edit installed components as if they were local source.

`modules/local/**` is repository-authored source. Read `modules/local/AGENTS.md` before changing it.
